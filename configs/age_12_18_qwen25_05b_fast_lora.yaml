# age_12_18 adapter — Qwen 2.5 0.5B, fast config (rank 4, 8 layers)
# 24 transformer layers total; 8 layers adapted (33%).
# Train with: mlx_lm.lora --config configs/age_12_18_qwen25_05b_fast_lora.yaml

model: "mlx-community/Qwen2.5-0.5B-Instruct-4bit"
train: true
fine_tune_type: "lora"
data: "data/age_12_18"

num_layers: 8

lora_parameters:
  rank: 4
  scale: 20.0
  dropout: 0.0

batch_size: 4
iters: 600
learning_rate: 1e-5

lr_schedule:
  name: cosine_decay
  warmup: 100
  arguments: [1e-5, 600]

mask_prompt: true

adapter_path: "adapters/fast/qwen25_05b/age_12_18"

save_every: 100
steps_per_report: 10
steps_per_eval: 60
val_batches: -1
max_seq_length: 768
seed: 42
